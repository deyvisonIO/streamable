"use client"

interface headerProps {
  hostIdentity: string,
  hostName: string,
  viewerIdentity: string,
  imageUrl: string,
  name: string,
  isFollowing: boolean,
}

export function Header({ hostIdentity, hostName, viewerIdentity, imageUrl, name, isFollowing  }:headerProps) {
  return (
    <div className="flex flex-col-1 lg:flex-col-1 lg:flex-row gap-y-4 lg:gap-y-0">Header Component</div>
  )
}
